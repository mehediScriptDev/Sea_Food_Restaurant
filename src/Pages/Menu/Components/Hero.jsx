import banner from '../../../../assets/menu/banner3.jpg'

const Hero = () => {
    return (
        <div style={{ backgroundImage: `url(${banner})` }} className='h-full bg-center bg-cover flex items-center justify-center mb-10 py-10'>
            
        </div>
    );
};

export default Hero;